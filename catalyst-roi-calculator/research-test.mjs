import { roiScenarios } from './src/data/roiScenarios.js'; import { generateResearchSummary } from './src/data/researchData.js'; let covered = 0; Object.keys(roiScenarios).forEach(id => { if (generateResearchSummary(id, 'retail')) covered++; }); console.log(`Research Coverage: ${covered}/${Object.keys(roiScenarios).length} (${((covered/Object.keys(roiScenarios).length)*100).toFixed(1)}%)`);
